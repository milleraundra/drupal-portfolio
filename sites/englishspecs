





Completed:

A user can enter a value, direction, and phrase
- 3
- right
- "Food"
On submit, the form redirects to a new page


/*Custom Validation*/
The string cannot contain numbers
The string cannot container characters other than spaces and punctuation. //regex


/*Functionality*/
The application drops an input to lower case
The application looks at one letter, finds the letter the given distance and direction away from it, and pushes that new value to an array.
The array is returned.

The program handles spaces (1)
The program handles punctuation (2)
Knows to wrap around the array for


? How would I maintain capitalization ?
